# Phase 2 Pilot Scenario: Java CRUD

Status: STRUCTURE_VALIDATED

Flow:

1. `prd-authoring` creates `docs/requirements/REQ-CRUD/prd.md`.
2. `design-doc` creates design with DB and Dubbo contracts.
3. `pre-coding-check` emits tasks.
4. `generate-tests` runs TDD mode.
5. `new-crud` consumes design and tasks.
6. Build/test/self-test evidence is recorded.
7. `tzh-review` consumes evidence.

Runtime execution in a business repository: NOT_EXECUTED.
